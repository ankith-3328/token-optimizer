[project]
name = "token-optimizer"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "sentence-transformers>=5.6.1",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
